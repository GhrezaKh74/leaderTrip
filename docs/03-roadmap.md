# ۳. فازبندی و نقشهٔ راه

هر فاز یک **خروجی قابل استفاده** دارد — نه یک تکهٔ نیمه‌کاره.
معیار پذیرش هر فاز پیش از رفتن به فاز بعد باید کامل باشد.

| فاز | نام | وضعیت |
|---|---|---|
| ۰ | پی‌ریزی | ✅ انجام شد |
| ۱ | هستهٔ برنامه‌ریز (MVP) | ✅ انجام شد |
| ۲ | هوشمندی: آب‌وهوا، مشاور، بودجه | ⏳ بعدی |
| ۳ | ابزارهای لیدر و خروجی | ⬜ |
| ۴ | حالت حین سفر | ⬜ |
| ۵ | دادهٔ باز و مسیر واقعی | ⬜ |

---

## فاز ۰ — پی‌ریزی ✅

**خروجی:** اسکلت پروژه و مستندات.

- [x] راه‌اندازی Vite + React 19 + TypeScript
- [x] Tailwind، فونت وزیرمتن، چیدمان راست‌به‌چپ، تم روشن/تاریک
- [x] مستندات: چشم‌انداز، ویژگی‌ها، معماری، مدل هزینه، الگوریتم، فازبندی

---

## فاز ۱ — هستهٔ برنامه‌ریز (MVP) ✅

**قول فاز:** کاربر ورودی‌ها را می‌دهد و یک برنامهٔ کامل روزبه‌روز با تفکیک هزینه و نقشه می‌گیرد.
**تخمین:** بزرگ‌ترین فاز — حدود ۵۵٪ کل کار.

### کارها

**داده**
- [x] `cities.ts` — ۸۵ شهر با مختصات، ضریب گرانی، سطح امکانات
- [x] `pois.ts` — ۱۴۳ جاذبه با همهٔ فیلدهای مدل داده
- [x] `vehicles.ts` — حدود ۱۵ پروفایل خودروی رایج ایران
- [x] `pricing.ts` — همهٔ ضرایب سند مدل هزینه

**موتور**
- [x] `geo.ts` — هاورساین، ضریب پیچش، مرکز ثقل
- [x] `sun.ts` — طلوع و غروب
- [x] `scoring.ts` — امتیازدهی مبتنی بر پروفایل گروه
- [x] `router.ts` — نزدیک‌ترین همسایه + ۲-opt
- [x] `planner.ts` — انتخاب مسیر (جهت‌یابی) + زمان‌بندی روزها
- [x] `cost.ts` — همهٔ اقلام هزینه با `formula` قابل نمایش

**کتابخانه**
- [x] `jalali.ts` — تبدیل شمسی/میلادی + نام ماه‌ها
- [x] `format.ts` — اعداد فارسی، تومان، مدت‌زمان
- [x] `storage.ts` — ذخیره و بازیابی سفرها

**رابط کاربری**
- [x] ویزارد ۵ گامی: مقصد ← تاریخ ← همسفران ← خودرو ← بودجه و سلیقه
- [x] صفحهٔ برنامه: تایم‌لاین روزها
- [x] پنل هزینه: نمودار تفکیک + جدول قابل بازکردن + ویرایش ضرایب
- [x] نقشهٔ Leaflet با نشانگرهای شماره‌دار و خط مسیر
- [x] کارت جاذبه با همهٔ برچسب‌های تناسب

**تست**
- [x] تست واحد برای `cost`، `geo`، `jalali`، `router`
- [x] CI: typecheck + lint + test + build

### معیار پذیرش فاز ۱ — ✅ محقق شد

> «تهران، ۳ روز، ۴ نفر (سنین ۳۵، ۳۳، ۸، ۶۷)، پژو ۲۰۶، بودجه ۲۵ میلیون، شعاع ۳۰۰ کیلومتر»
> ⇒ یک برنامهٔ ۳ روزهٔ منطقی با ۶ تا ۹ جاذبه، تفکیک کامل هزینه، نقشه، و بدون روز نشدنی.

این سناریو به‌صورت تست خودکار در `src/engine/__tests__/engine.test.ts` اجرا می‌شود.

**دو تصمیم مهم که حین ساخت گرفته شد و در سند اولیه نبود:**

۱. **انتخاب جاذبه‌ها یک مسئلهٔ «جهت‌یابی» است، نه رتبه‌بندی.** برداشتن N جاذبهٔ
   با بالاترین امتیاز، مجموعه‌ای پخش‌شده در چند جهت مخالف می‌دهد که در یک سفر
   جاده‌ای اصلاً شدنی نیست. الگوریتم به «درج حریصانه با نسبت امتیاز به زمان
   اضافه‌شده» تغییر کرد تا مسیر خودبه‌خود در یک راستا شکل بگیرد.

۲. **جاذبه‌های شهر خودِ مسافر در سفر چندروزه کنار گذاشته می‌شوند.** هزینهٔ
   رسیدن به آن‌ها صفر است، پس هر معیار «ارزش به ازای زمان» آن‌ها را برنده
   می‌کند و کل برنامه را می‌بلعند. در سفر یک‌روزه اما گشت شهری مجاز است.

---

## فاز ۲ — هوشمندی

**قول فاز:** برنامه به شرایط واقعی واکنش نشان می‌دهد.

- [ ] `services/weather.ts` — Open-Meteo با مدیریت خطا و کش
- [ ] پیش‌بینی ۱۶ روزه + میانگین تاریخی برای تاریخ‌های دورتر
- [ ] بازچینش برنامه بر اساس آب‌وهوا (گام ۶ الگوریتم)
- [ ] `advisor.ts` — همهٔ هشدارهای سه‌سطحی
- [ ] موتور بهینه‌سازی بودجه با شش اهرم کاهش
- [x] سناریوهای خوش‌بینانه/محتمل/بدبینانه  ← زودتر از موعد، در فاز ۱ انجام شد
- [x] تقسیم هزینه بین اعضا (سه حالت)  ← زودتر از موعد، در فاز ۱ انجام شد
- [ ] `packing.ts` — چک‌لیست هوشمند
- [ ] پین/بلاک کردن جاذبه‌ها
- [x] توقف‌های خودکار استراحت و سوخت‌گیری  ← زودتر از موعد، در فاز ۱ انجام شد

**معیار پذیرش:** همان سفر نمونه در یک روز بارانی، برنامهٔ متفاوتی تولید کند و دلیلش را بگوید.

---

## فاز ۳ — ابزارهای لیدر و خروجی

- [ ] ویرایش دستی برنامه (کشیدن و رها کردن بین روزها)
- [ ] افزودن جاذبه یا توقف دلخواه
- [ ] نسخهٔ چاپی/PDF
- [ ] اشتراک‌گذاری با لینک (فشرده‌سازی حالت در URL)
- [ ] خروجی/ورودی JSON
- [ ] PWA + کش آفلاین + قابلیت نصب
- [ ] کارت اطلاعات اضطراری
- [ ] مدیریت چند سفر (بایگانی، کپی، تغییر نام)

**معیار پذیرش:** برنامه روی گوشی نصب شود، در حالت هواپیما باز شود و لینکش برای دوستان کار کند.

---

## فاز ۴ — حالت حین سفر

- [ ] چک‌این هر توقف با ثبت ساعت واقعی
- [ ] ثبت هزینهٔ واقعی و مقایسهٔ زنده با تخمین
- [ ] تسویه‌حساب گروهی («چه کسی به چه کسی چقدر بدهکار است»)
- [ ] دفترچهٔ خاطرات با یادداشت و عکس
- [ ] پشتیبانی از کاروان چندخودرویی
- [ ] نقش‌ها و شماره تماس اعضا

**معیار پذیرش:** پس از سفر، گزارش «تخمین در برابر واقعیت» تولید شود.

---

## فاز ۵ — دادهٔ باز و مسیر واقعی

- [ ] غنی‌سازی جاذبه‌ها از OpenStreetMap Overpass
- [ ] مسافت و زمان واقعی جاده‌ای با OSRM (با بازگشت به تخمین در نبود شبکه)
- [ ] پروفایل ارتفاعی مسیر
- [ ] رابط انگلیسی
- [ ] یادگیری از بازخورد کاربر روی وزن‌های امتیازدهی

---

## ریسک‌ها و پاسخ ما

| ریسک | اثر | پاسخ |
|---|---|---|
| قیمت‌ها در ایران سریع عوض می‌شوند | تخمین‌ها بی‌اعتبار می‌شوند | همهٔ ضرایب در UI قابل ویرایش + نمایش تاریخ به‌روزرسانی |
| فاصلهٔ هوایی ≠ فاصلهٔ جاده‌ای | خطای مسافت و سوخت | ضریب پیچش منطقه‌ای؛ OSRM در فاز ۵ |
| جمع‌آوری دستی دادهٔ جاذبه‌ها زمان‌بر است | کندی فاز ۱ | شروع با ۱۲۰ جاذبهٔ باکیفیت، نه ۱۰۰۰ مورد بی‌کیفیت |
| بدون سرور یعنی بدون همگام‌سازی بین دستگاه‌ها | محدودیت کاربر | اشتراک‌گذاری با لینک و خروجی JSON در فاز ۳ |
| Open-Meteo از دسترس خارج شود | نبود آب‌وهوا | افت مهربانانه؛ اپ بدون آن کامل کار می‌کند |
